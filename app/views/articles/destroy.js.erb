$(document).ready(function() {
  $('#articles-article').remove();
  $('#errors-messages').remove();
  $('#articles-list').prepend('<%= escape_javascript(render('articles/article', obj: @articles)) %>');
  $('#articles-list').prepend('<%= escape_javascript(render('layouts/messages')) %>');
});